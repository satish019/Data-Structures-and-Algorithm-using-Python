# Taking User Input Base in Variable b
b = float(input('Enter the Base : '))

# Taking User Input Base in Variable b
e = float(input('Enter the Exponent : '))

# Base b raised to power of Exponent e is presented as b**e
print (b**e)
